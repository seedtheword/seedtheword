# Editor / IDE metadata — local only
.kiro/
.vscode/
.idea/
*.code-workspace

# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Backup files
*.bak
*.orig
*~

# Logs
*.log

# Dependency artifacts (if we ever add a build step)
node_modules/
__pycache__/
*.pyc

# Large local-only video files (host these on Drive / YouTube and embed
# instead — anything over a few MB shouldn't be committed). Add specific
# filenames here as they come up.
assets/videos/cant-be-neutral.mp4
